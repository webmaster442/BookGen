<UserControl
    x:Class="BookGen.Launcher.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel>
        <TextBlock
            Style="{StaticResource HeaderText}"
            Text="Settings" />
        <CheckBox
            Margin="50,5"
            Content="Use Windows terminal, when launching terminal (Requires Windows Terminal to be installed)"
            FontSize="15"
            IsChecked="{Binding UseWindowsTerminal}" />
        <CheckBox
            Margin="50,5"
            Content="Automatically exit launcher, when opening folder in VS Code or Terminal"
            FontSize="15"
            IsChecked="{Binding AutoExitLauncher}" />
    </StackPanel>
</UserControl>
